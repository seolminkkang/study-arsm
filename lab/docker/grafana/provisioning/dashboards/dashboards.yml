apiVersion: 1

providers:
  - name: lab
    orgId: 1
    folder: lab
    type: file
    disableDeletion: false
    # 당일에 화면을 만지다 실수해도 파일이 원본이다.
    allowUiUpdates: true
    updateIntervalSeconds: 10
    options:
      path: /var/lib/grafana/dashboards
      foldersFromFilesStructure: false
