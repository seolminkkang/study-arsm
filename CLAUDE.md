# backend-study

『주니어 백엔드 개발자가 반드시 알아야 할 실무 지식』(최범균, 한빛미디어) 2인 스터디.
책을 읽고 요약하는 스터디가 아니라, **측정해서 확인하는 스터디**다.

## 이 vault를 읽는 사람

두 명 다 이렇다. 노트를 쓸 때 이 전제를 지킨다.

- **비전공자.** 운영체제, 네트워크, DB를 훑어본 적 있는 정도다.
  용어를 들으면 낯설지는 않지만, 그게 언제 어떻게 문제가 되는지는 모른다.
- **실무 경험 없음.** 장애를 겪어본 적이 없다.
  "서비스가 느려지는 상황"을 상상으로만 안다.
- 코드는 쓸 수 있다. 스프링으로 기능 구현은 해봤다.

### 그래서 노트를 쓸 때

- **용어는 처음 나올 때 풀어쓴다.**
  "커넥션 풀에서 커넥션을 얻지 못해 대기 중인 스레드 수(pending)"처럼.
  약어는 반드시 한 번 펼친다. `TTFB(Time to First Byte)`
- **"당연히", "아시다시피", "보통은 이렇게 합니다"를 쓰지 않는다.**
  실무 관례를 전제로 깔면 두 사람 다 모른다. 근거나 출처를 댄다.
- **"이런 상황 겪어보셨을 텐데" 류를 쓰지 않는다.** 겪어본 적이 없다.
  대신 이번 실험에서 직접 만든 상황을 근거로 삼는다.
- **직관에 기대지 않고 숫자로 쓴다.** "많이 느려졌다"가 아니라 "p95가 40ms → 480ms".
  이 책은 산술 모델을 명시적으로 준다(`풀 크기 / 쿼리 시간 = 최대 TPS`).
  직관이 없어도 예측과 검증이 가능하다는 뜻이므로 그 방식을 최대한 활용한다.
- **모르는 걸 모른다고 적을 자리를 반드시 남긴다.**
  개념 노트의 "아직 모르는 것" 섹션을 비워두지 않는다.
  애매하게 이해한 것을 이해한 것으로 넘기는 게 이 스터디에서 가장 위험하다.

### 문장 수준

`technical-writing-guide` skill의 **Step 3(문장 다듬기)** 을 따른다.
주체 분명하게, 한 문장에 하나의 생각, 구체적인 수치, 자연스러운 한국어, 일관된 용어.

Step 1(문서 유형 분류)은 실험 노트에 적용하지 않는다. 네 유형 어디에도 맞지 않는다.
`output/`의 공개용 글에는 Step 1~3 전체를 적용한다.

단, **당일 20분 안에 쓰는 초안은 못생겨도 된다.** 문장 다듬느라 시간을 쓰면
다음 회차엔 아예 안 쓰게 된다. 기록이 안 남는 게 못생긴 기록보다 훨씬 나쁘다.
당일에 지킬 것은 "구체적인 수치를 쓴다" 하나면 충분하다. 다듬기는 나중 일이다.

## 이 저장소의 두 축

| 폴더 | 역할 |
|---|---|
| `vault/` | 옵시디언 vault. 실험 기록, 개념 노트, 회차 기록 |
| `lab/` | 실험 환경. 스프링 앱, 시딩 SQL, k6 스크립트, docker |

`vault/`가 주(主)고 `lab/`은 그 근거다. 실험 노트는 항상 `lab/`의 커밋 해시를 참조한다.

## 지식의 세 층 — 절대 섞지 않는다

```
raw/         측정 원본 (k6 출력, EXPLAIN 결과, Grafana 캡처)
             → 한 번 쓰면 수정하지 않는다. 판단의 기준점이다.

concepts/    개념 노트 (원자 단위)
experiments/ 실험 노트 (가설 → 조건 → 결과 → 해석)
planning/    회차 전 준비물 (plan / cheatsheet / overview.html)
sessions/    회차 후 기록
             → 계속 다듬는다.

output/      공개용 글
             → 위 셋에서 파생된다.
```

회차 준비물은 `planning/`, 진행 기록은 `sessions/`에 둔다.
`planning/`에는 회차당 3개를 만든다 — `plan.md`(계획) / `cheatsheet.md`(당일 실행) /
`overview.html`(슬라이드). **개관을 md로 따로 만들지 않는다. 슬라이드와 중복이다.**

**회차당 이 3개만 만든다.** Challenger에게 미리 보낼 사전 공지도 파일로 만들지 않는다 —
`plan.md`의 실험 조건 부분을 복사해서 보내되 **결과와 힌트는 뺀다.**
파일로 두면 원본이 바뀔 때 그것만 옛 내용으로 남는다.

**회차 노트는 그 회차의 허브다.** 맨 위에 "이 회차의 문서" 표를 두고
준비물·실험·raw·개념을 전부 링크한다.
폴더를 회차별로 나누지 않는다 — 개념과 raw는 회차를 가로지른다.

`raw/`를 고치면 그때 실제로 무엇이 측정됐는지 확인할 방법이 사라진다.
해석이 바뀌면 `experiments/`를 고치고 `raw/`는 그대로 둔다.

`raw/`는 AI가 만든 게 아니라 측정 도구가 뱉은 원본이다.
**두 사람이 직접 읽어야 하는 자료다.** 실행계획 읽는 법을 익히는 것이
3장의 절반이므로 요약본만 보고 넘어가지 않는다.

## 절대 규칙

1. **실측 없이 결론을 쓰지 않는다.** 추측은 `#가설` 태그로 남기고 결론과 구분한다.
2. **예측은 사람이 쓴다.** 실험 노트의 "예측" 섹션을 AI가 채우지 않는다.
   예측이 틀리는 순간이 학습이 일어나는 유일한 지점이다.
3. **모든 결론은 재현 가능해야 한다.** 커밋 해시, 데이터 건수, 실행 조건이 노트에 있어야 한다.
4. **`raw/` 안의 파일은 수정하지 않는다.** 추가만 한다.
5. **실패한 실험도 남긴다.** `status: failed`로 두고 삭제하지 않는다.
   왜 실패했는지(차이가 안 남 / 환경 문제 / 조건 설계 오류)를 적는다.
   "이 방법으로는 관측되지 않는다"도 결과다.
6. **실험 번호는 Host가 부여한다.** 회차 시작 전에 그 회차에서 쓸 번호를
   미리 잡아둔다(1회차 = exp-001~010). 두 사람이 동시에 만들어 충돌하는 걸 막는다.
7. **실험이 만든 것은 그 실험이 끝나는 즉시 지운다.**
   다음 실험으로 넘어가기 전에 원복하고 확인한다.
   남아 있으면 다음 실험의 실행계획이 달라져 결과를 신뢰할 수 없다.
   칼럼을 추가하고 대량 UPDATE를 한 경우에는 `DROP COLUMN`만으로 부족하다 —
   테이블이 부풀어 있으므로 `VACUUM FULL`까지 해야 원래 상태로 돌아온다.

## git

### 커밋 메시지

`<타입>: <내용>` 형식, 한국어. 타입 6개만 쓴다.

| 타입 | 뜻 |
|---|---|
| `setup` | 환경 구축 (docker, 스키마, 시딩) |
| `lab` | 실험 코드 변경 (app, k6, sql) |
| `exp` | 실험 실행 직전 상태 고정 |
| `note` | vault 문서 작성/수정 |
| `fix` | 버그 수정 |
| `chore` | 그 외 |

예시:
```
setup: PostgreSQL 컨테이너와 테이블 4개 생성
lab: /lab/ratings 커서 페이징 엔드포인트 추가
exp: exp-001 조건 B (커넥션 풀 10)
note: exp-001 실험 노트 작성
```

### exp 커밋이 중요한 이유

실험 노트 frontmatter의 `commit:` 해시를 따라갔을 때
"이 숫자를 낸 코드와 설정이 이 상태였다"가 재현되어야 결론이 검증된다.
설정만 바꾸는 경우에도(application.yml 풀 크기) 반드시 커밋한다.
커밋 없이 측정하면 그 결과는 재현 불가능한 주장이 된다.

측정 → exp 커밋 → 노트에 해시 기록. 이 순서를 지킨다.
`git log --oneline | grep "exp:"` 로 측정 시점만 추릴 수 있다.

### 2인 협업

- 작업 시작 전 항상 `git pull`
- Host가 회차를 준비하는 동안 Challenger는 `lab/`을 건드리지 않는다
- 충돌 시 `raw/`는 양쪽 다 살린다. 측정 기록은 지우지 않는다

## ponytail

코드를 최소한으로 쓰게 만드는 플러그인. 이 저장소에서는 켜고 쓴다.
`lab`은 의도적으로 최소여야 하므로 방향이 일치한다.

### 적용하는 곳

`lab/app/` — 파일 5개, 계층 없음이 목표다.
스프링은 관례가 무거워서 AI가 자동으로 3계층, DTO, Mapper,
@ControllerAdvice를 만들어낸다. 그러면 응답 시간에 프레임워크 오버헤드가 섞여
"쿼리가 느린 건지 프레임워크가 느린 건지" 구분할 수 없게 된다.
병목을 하나만 만들어야 하는 실험에서 치명적이다.

### 반드시 끄는 곳 — 두 군데

**1. `lab/sql/03_seed_ratings.sql`**

시딩의 치우친 분포는 의도적 복잡도다.

```
헤비 100명 → 각 5,000건
중간 1만 명 → 각 300건
라이트 9만 명 → 각 17건
```

ponytail 관점에서 이건 명백히 과한 코드로 보인다.
"random() * 100000 하면 되는데 왜 CASE를 세 개나?"로 단순화될 수 있다.

**균등 분포로 시딩하면 선택도 실습이 통째로 죽는다.**
누구를 조회하든 건수가 같아져서 실행계획에 차이가 안 생긴다.
B-2 실험(헤비 5,000건 vs 라이트 17건)이 성립하지 않는다.

**2. `lab/k6/`**

워밍업 60초는 낭비처럼 보이지만 JVM JIT 컴파일 때문에 필수다.
초반 수십 초는 인터프리터로 돌아서 실제보다 몇 배 느리다.
이 구간을 빼면 모든 측정이 거짓말이 된다.

도착률(arrival-rate) 기반 시나리오도 VU 기반보다 복잡해 보이지만,
VU 기반으로 단순화하면 서버가 느려질 때 부하도 같이 줄어들어
포화점이 아예 관측되지 않는다.

### 작업 시 규칙

위 두 파일을 만들거나 고칠 때는 프롬프트에 명시한다:

> 이건 의도적 복잡도다. 단순화하지 마.

## 스터디 형식

역할을 매 회차 번갈아 맡는다.

- **Host** — 책을 미리 읽는다. 하는 일은 요약 발표가 아니라 **관찰 조건 설계**다.
  어떤 숫자를 화면에 띄울지, 그중 어떤 조합이 직관에 안 맞게 보일지를 고른다.
  당일 전에 실험을 혼자 돌려보고 차이가 나는지 확인한다(파일럿).
- **Challenger** — 책을 안 읽고 온다. 관찰하고, 예측하고, 틀리고, 그 다음에 읽는다.
  사전 과제: 실행 환경을 자기 노트북에 미리 띄워보고 오기.

### 회차 흐름 (2시간)

| 시간 | 내용 |
|---|---|
| 0:00–0:15 | 지난 회차 미해결 개념 확인 |
| 0:15–0:40 | **관찰** — 실행하고 숫자만 본다. "이 중에 뭐가 이상해?" |
| 0:40–1:05 | **개념 도입** — 그 이상함에 이름 붙이기. Host가 책 내용을 꺼냄 |
| 1:05–1:25 | **예측** — 조건을 바꾸면 어떻게 될까. 둘 다 글로 적는다 |
| 1:25–1:50 | **실행** — 맞는지 확인 |
| 1:50–2:00 | 노트 정리, 다음 질문 추출 |

예측이 개념 도입 **뒤에** 오는 이유가 있다. 어휘가 없는 상태에서는 예측이 불가능하다.
"CPU 유휴시간을 모르는데 그것 때문에 뭐가 생길지 예측해봐"는 성립하지 않는 요구다.

현상을 먼저 보고 → 이상한 점을 찾고 → 거기에 이름을 붙이면,
개념이 정의가 아니라 **이름 붙이기**로 들어온다.
예를 들어 CPU 사용률은 40%인데 응답 시간이 계속 느려지는 걸 먼저 보면
"CPU가 노는데 왜 느리지?"라는 의문이 생기고, 그때 유휴시간 개념이 필요해진다.
개념을 먼저 배우고 현상을 찾는 것보다 이 순서가 비전공자에게 훨씬 잘 붙는다.

### 확인 방식 — A급 / B급 / C급

**중요도 순이 아니다.** 어떻게 확인할 수 있는지로 나눈 것이다.
한 회차에 다루는 양을 제한한다. 다 하려 하면 각각 5분씩 훑고 지나가게 되고,
그건 처음에 피하려던 강의식으로 돌아가는 것이다.

- **A급 = 부하 실험** — 트래픽을 주고 조건을 바꿔가며 before/after를 잰다. 회차의 중심. 2개
- **B급 = 단건 확인** — 한 번 실행하고 결과를 읽는다. 각 5분. 3~4개
  (`EXPLAIN`은 그 한 예일 뿐이다. 4장부터는 로그와 단건 응답시간으로 확인한다)
- **C급 = 읽고 넘김** — 언급만 하고 `#미실험` 태그

A·B·C는 방식의 약칭이고 등급이 아니다. 실험 번호(`A-1`, `B-3`)가 이 letter를
쓰고 있어서 이름만 바꾸면 노트와 링크가 흔들린다. 표기는 그대로 둔다.

**C급을 넘기는 데 죄책감을 갖지 않는다.** C급에도 중요한 절이 많다 —
캐시, GC, 대기 처리는 다 중요한데 로컬에서 재현이 안 되거나 시간이 없어서 미룬 것이다.
넘긴 이유(`시간부족` / `재현위험` / `환경없음` / `뒷장재등장`)를
`progress.md`에 적어두면 나중에 돌아올 수 있다.

## 진행 상태

`vault/progress.md`에 책 전체 목차와 각 절의 상태가 있다.
"다음에 뭘 할까"를 물으면 그 파일을 먼저 읽는다.

회차가 끝나면 README 회차 목록 표에 행을 추가한다.
"이번에 알게 된 것"은 한 문장으로 쓰고 자세한 건 링크로 보낸다.
**README에 회차 내용을 풀어 쓰지 않는다 — 장기로 갈 저장소다.**
블로그 글을 쓰면 "글" 컬럼에 링크를 채운다.

## 원본 프로젝트 참고 범위

`lab/`은 Moha Cinema 프로젝트의 **DB 스키마와 영화 데이터만** 재활용한다.

**참고할 것**
- `exec/sql_dump/` 의 스키마와 인덱스 정의
- `MovieController`, `RatingController` 의 조회 쿼리 패턴

**참고하지 말 것**
- 아키텍처, 계층 구조, DTO, Security, OAuth, MyBatis, 추천 서비스(FastAPI)

`lab`은 의도적으로 최소한이어야 한다. 파일 5개, 의존성 5개.
컨트롤러에서 리포지토리를 직접 불러도 된다. 실험용이므로 구조가 못생겨도 무방하다.
원본 구조를 따라가면 파일 5개가 30개가 되고, 그러면 응답 시간에
Security 필터 체인과 프레임워크 초기화가 섞여 병목 구분이 불가능해진다.

원본은 건드리지 않는다. 사본에서 작업하고, 실습용 DB 컨테이너도 따로 띄운다.
인덱스를 지웠다 걸었다 하고 500만 건을 밀어넣을 것이므로 분리돼야 한다.

## 1회차 실험 명세

`vault/planning/001-plan.md` 에 A급 2개와 B급 4개의 조건·절차·판정 기준이 있다.
리허설과 당일 모두 그 문서를 따른다.

절별 확인 방식 배정과 넘긴 사유는 `vault/progress.md` 에 있다.

## 지금 단계

`lab/README.md`의 구축 순서를 따른다. 현재 ①번부터 시작.
