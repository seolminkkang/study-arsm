# lab/docker/docker-compose.yml 이 읽는 값.
# 복사해서 .env로 저장한다 (SETUP.md 1단계).
#   cp .env.example .env
#
# docker compose는 실행한 디렉터리 기준으로 .env를 찾으므로,
# lab/docker/ 안에서 실행한다면 이 파일도 lab/docker/.env로 복사한다.

POSTGRES_DB=labdb
POSTGRES_USER=lab
POSTGRES_PASSWORD=lab
POSTGRES_PORT=5433
